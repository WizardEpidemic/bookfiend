# Defines the container used to run the BookFiend Next.js frontend.

FROM node:24-alpine

WORKDIR /app

COPY apps/web/package*.json ./

RUN npm ci

COPY apps/web .

EXPOSE 3000

CMD ["npm", "run", "dev", "--", "-H", "0.0.0.0"]